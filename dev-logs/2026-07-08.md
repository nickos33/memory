# 开发日志 — 2026-07-08

## 完成事项

### 桌面悬浮小组件
- 深色毛玻璃风格悬浮面板（`rgba(42,58,80,0.78)`），默认 260×190，左上角显示
- 显示所有纪念日（含过去），按日期升序，过去的半透明显示
- 卡片式布局，每条有独立的圆角背景，区分度清晰
- 标题"Memory"（花体），居中显示
- 拖拽移动，位置持久化到 `widget-config.json`
- 右键菜单：打开主窗口 / 恢复全部隐藏 / 隐藏面板
- 右键点击单条纪念日 → 显隐切换
- 底部"已隐藏 N 个 · 点击恢复全部"一键恢复
- 主窗口"面板"按钮：点击切换小组件显示/隐藏
- 隐藏滚动条，小组件固定大小（不可调整）
- 移除红绿灯按钮（`trafficLightPosition: { x: -100, y: -100 }`）

### UI 调整
- 主界面和小组件标题统一改为花体"Memory"
- 搜索和新增按钮统一蓝色胶囊样式
- 卡片悬浮菜单（置顶/编辑/删除）统一右对齐

### Bug 修复
- 拖拽排序：`store.mjs` 添加 `reorder()` + `order` 字段
- Dock 图标变大：移除运行时 `app.dock.setIcon()`
- 小组件关闭/显示逻辑：toggle/hide 统一用 `hide()` + `show()`
- 隐藏纪念日恢复：底部按钮 + 右键"恢复全部隐藏"
- 打包闪退：`main.mjs` toggleWidget 语法错误修复
- 小组件 `closable/minimizable/maximizable: false` 移除红绿灯

### 应用配置
- 开机自启动：`app.setLoginItemSettings({ openAtLogin: true })`
- 通知：node-cron 每天 9 点，提前 0/1/3/7 天
- 导出/导入：`store.mjs` 实现 `exportData()` / `importData()`
- 图标：IMG_6850.JPG 居中裁剪 1024×1024，72 DPI

## 文件清单

| 文件 | 说明 |
|------|------|
| `main.mjs` | 主进程，含 createWidget + Widget IPC |
| `main.cjs` | CJS 备用入口 |
| `preload.cjs` | widgetAPI + memorialAPI |
| `store.mjs` | 数据存储（CRUD + reorder + export/import） |
| `notifications.mjs` | 通知模块 |
| `src/renderer/widget/widget-index.js` | 小组件 React 组件 |
| `src/renderer/widget/Widget.css` | 小组件样式 |
| `public/widget.html` | 小组件 HTML 入口 |
| `webpack.config.cjs` | 双入口（renderer + widget） |
| `widget-config.json` | 小组件位置/大小配置 |

## 已知问题

- `ELECTRON_RUN_AS_NODE` 环境变量阻止 electron 二进制下载
- `node_modules/electron/index.js` 遮蔽 Electron 内置模块

## Git 版本

- `v1.0.0`: 基础功能版本（纪念日 CRUD + 农历 + 通知）
- `v1.1.0`: 桌面悬浮小组件 + Memory 品牌升级
- `v2.0.0`: Electron 二进制修复 + 正常退出逻辑
